請生成一個 Jupyter notebook，步驟如下：

1. 產生300個0到1000之間的隨機數據 \(X\)。
2. 根據規則設定 \(Y\)：當 \(X\) 在500到800之間時 \(Y=1\)，否則 \(Y=0\)。
3. 使用邏輯回歸進行預測，並將預測結果存儲為 \(y1\)。
4. 使用支持向量機（SVM）進行預測，並將預測結果存儲為 \(y2\)。
5. 重新排列 \(Y\) 和 \(y1\) 的順序，根據 \(X\) 繪製 \(Y\) 和 \(y1\)，並用綠色線顯示邏輯回歸預測。
6. 重新排列 \(Y\) 和 \(y2\) 的順序，根據 \(X\) 繪製 \(Y\) 和 \(y2\)，並用紅色線顯示 SVM 預測。

確保使用適當的 Python 庫進行數據分析和繪圖，並添加必要的註解和說明。
